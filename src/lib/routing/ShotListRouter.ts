/**
 * Shot-list orchestration (script → shots → parallel model dispatch).
 * Canonical import path; implementation lives in swarm/ during migration.
 */
export { SwarmRouter as ShotListRouter, SwarmRouter } from '../swarm/SwarmRouter'
