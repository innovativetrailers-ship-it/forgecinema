/** @deprecated Import from `@/lib/models/resolve` — re-export shim for existing imports. */
export {
  ModelResolveError,
  assertModelsResolvable,
  falBaseApp,
  resolveModel,
  resolveVideoEndpoint,
  type ModelDef,
} from './resolve'
export type { Provider } from './registry'
